# MOLEP 프로젝트 핸드오버 문서 (이어받기용 통합본)

> 작성일: 2026-04-19
> 목적: **회사 PC의 다른 Claude 계정**에서 이 프로젝트를 이어받아 작업할 수 있도록, 산출물 + 대화 이력 + 유저 컨텍스트 + 피드백 룰을 한 파일에 통합

---

## 0. 이어받는 쪽에서 바로 해야 할 일 (Quick Start)

이 레포는 **Public**이므로 인증/초대 없이 clone만 하면 됩니다.

### Step 1: clone
```bash
git clone https://github.com/yourjini/molep.git
cd molep
```

### Step 2: 본 문서 정독
- `docs/HANDOVER.md` (본 문서) — 전체 맥락
- `docs/requirements-v1.md` — 요건 정의서

### Step 3: 현재 프로토타입 확인
- 브라우저에서 https://molep.vercel.app 접속

### Step 4: 유저에게 다음 작업 확인 후 시작
- **읽기 전용 환경입니다** — 회사 PC에서는 push 권한이 설정되지 않았습니다
- 수정 작업은 로컬에서만 하고, 집 PC로 돌아와서 최진희 계정으로 push합니다

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | **MOLEP** (글로벌 게임 포털 가입/로그인 플로우 프로토타입) |
| 목적 | 차주 내부 회의 **시연용** 프로토타입 |
| 대상 국가 | 한국(KR), 미국(US), 대만(TW), 일본(JP), 중국(CN) |
| 핵심 주제 | 국가별 법규 준수 + 통합 가입/로그인 플로우 + IP 국가 감지/검증 |
| 기획 근거 | 2026-04-17 내부회의 확정사항 |

---

## 2. 작업 위치 & 배포

| 항목 | 값 |
|---|---|
| 집 PC 로컬 경로 | `D:/MyC/molep` |
| 회사 PC 로컬 경로 | `C:/autoqa/molep` (평탄화 후) |
| GitHub (Public) | https://github.com/yourjini/molep |
| Vercel (프로덕션) | https://molep.vercel.app |
| 배포 방식 | GitHub `main` push → Vercel 자동 배포 |

---

## 3. 디렉토리 구조 (2026-04-19 개편)

```
molep/
├── CLAUDE.md                  # Claude Code 프로젝트 메모리 (자동 로드)
├── .gitignore
├── docs/
│   ├── PLAN.md                # 신규 프로토타입 설계서 (Single Source of Truth)
│   ├── HANDOVER.md            # 본 문서 (세션 히스토리)
│   └── requirements-v1.md     # 초기 요건 정의서 (v1, 참고용)
├── auth/                      # ★ 현재 작업 영역 — PLAN에 따라 1단계부터 구축
│   └── (빈 상태, PLAN 구현 단계 1~7로 채울 예정)
└── _legacy/                   # 구 프로토타입 (플로우 A/B 비교 UI) — 참고 보존
    ├── index.html             # (444 lines)
    ├── css/                   # main.css, components.css
    ├── js/                    # app.js, config.js, i18n.js, review-data.js
    └── vercel.json
```

### 새 프로토타입(auth/) 예정 구조 (PLAN.md §"디렉터리 구조" 참조)

```
auth/
├── index.html                 # SPA 쉘 (진입점)
├── README.md
├── assets/
│   ├── css/                   # base.css, components.css, risk-panel.css
│   ├── js/                    # app.js, state.js, router.js, i18n.js, mock-geoip.js, risk-overlay.js
│   ├── data/                  # countries.json, terms/*.md, admin-content.json, risks.json
│   └── i18n/                  # ko/en/zh-TW/ja/zh-CN.json
└── docs/
    ├── policy-matrix.md
    └── risks-register.md
```

---

## 4. 확정된 핵심 요건 (2026-04-17 내부회의)

1. **IP 국가 감지**: MaxMind GeoIP2 DB, 주 2회 자동 갱신 (Cloudflare CF-IPCountry 헤더 fallback)
2. **기본 언어**: **한국어 고정** (모든 국가 동일). 구글 번역 위젯으로 다국어 처리
3. **가입 플로우**: 국가 선택 + 쿠키 검증 (IP와 쿠키 불일치 시 재선택 유도)
4. **Auth 연동**: 프론트 쿠키의 국가 값을 Auth 서버에 전달 (Auth는 직접 국가 확인 불가)
5. **어드민**: 콘텐츠 노출 국가 필터링 (국가별 콘텐츠 분기)

### 국가별 인증 수단 & 연령 제한

| 국가 | 인증 수단 | 최소 연령 |
|---|---|---|
| KR | 네이버, 이메일 | 만 14세 |
| US | Google, Apple, X | 만 13세 |
| TW | Google, Apple, X | 만 18세 |
| JP | Google, Apple, X | 만 18세 |
| CN | WeChat, QQ | 만 18세 |

---

## 5. 구현된 핵심 기능 (현재까지)

- **플로우 A (IP 고정)**: 감지된 IP 국가를 그대로 적용, 사용자 변경 불가
- **플로우 B (수동 변경)**: 사용자가 국가를 수동 선택/변경 가능
- 두 플로우의 **단계별 비교 UI** (`index.html`)
- 5개국 설정 데이터 (`js/config.js`)
- **리스크 매트릭스** (법규/UX/기술 리스크 시각화)
- **검토 데이터**: 기획/정책/설계/개발/QA 각 관점의 고려사항 (`js/review-data.js`)

---

## 6. 작업 이력 (대화 히스토리 요약)

### 2026-04-17 (세션 1) — MOLEP 프로토타입 초기 구축
- Gemini 리서치 자료 기반 5개국 비교 분석
- config.js + review-data.js + i18n.js 구조 잡기
- 플로우 A/B 비교 UI 구현
- 커밋: `324bdd9 feat: MOLEP 글로벌 게임 포털 가입/로그인 플로우 프로토타입`

### 2026-04-18 (세션 2) — 내부회의 확정사항 반영 + Vercel 배포
- 내부회의(4/17) 확정사항을 requirements-v1.md로 문서화
- 기본 언어를 한국어 고정으로 변경 (구글 번역 위젯으로 다국어 대응)
- 국가별 콘텐츠 필터링 로직 추가
- Vercel 배포 연결 (GitHub 자동 배포)
- 커밋: `fc91617`, `705ba11`

### 2026-04-19 (세션 3) — 핸드오버 문서 정리 + 신규 플랜 반영
- 회사 PC에서 이어 작업하기 위한 HANDOVER.md 작성 및 통합
- **회사 PC 폴더 평탄화**: `C:/autoqa/molep/molep/*` → `C:/autoqa/molep/`로 한 단계 올림
- `.gitignore` 신규 추가 (`.claude/`, OS 임시파일, 참고 파일)
- **신규 프로토타입 플랜 반영**: `CLAUDE.md` + `docs/PLAN.md` 수립 → 구 프로토타입은 `_legacy/`로 이관, `auth/`를 신규 작업 영역으로 비움
- **작업 방침 변경**: 기존 "플로우 A/B 비교 UI" → "미로그인 접근 + 회원가입 풀 플로우 + 리스크 오버레이" (PLAN.md 기준)

### 2026-04-19 (세션 4) — 시연 프로토타입 v1 빌드 + 디자인·UX 반복 개선 + Vercel 배포 설정
**산출물**: `auth/index.html`, `auth/assets/css/main.css`, `auth/assets/js/{app,data,review-data,memo}.js`, 루트 `vercel.json`

**기능**
- 가입 플로우 9스텝: 국가 선택 → 가입 수단 → OAuth 시뮬 → 약관 → (KR 스킵) 생년월일 → 보호자 → 본인인증 → CN anti-addiction → 완료
- 로그인 화면 (`#/login`) — 국가별 가입 수단으로 분기, KR 이메일 폼 별도
- 5개국 분기: KR(네이버/이메일 + PASS·토스 본인인증), US(Google/Apple/X), TW/JP(LINE 등), CN(WeChat/QQ + 실명 + 방침미)
- KR 본인인증: PASS / 토스 두 버튼 (브랜드 컬러 적용, 생년월일 포함 시뮬레이션 → DOB 스킵)
- 데모 GNB 국가 강제 변경 + KR/EN 토글, 헤더 `접속국가:` 칩
- 약관 화면: 5개국별 약관 항목 + "보기" 클릭 시 우측 슬라이드 패널로 샘플 원문 표시 (`TERM_SAMPLES`)

**고려사항(`#/review`) 페이지**
- 8개 탭: 기획 / 정책·약관 / 설계·UX / 개발 / QA / 리스크 / 문의사항 / DB
- 각 항목: 레벨 + 국가 태그 + (옵션) "자세히 보기" → 우측 슬라이드 패널 (문제/제안/타사 대응 구조 일부 작성)
- 항목별 누적 피드백 메모 (localStorage, 시연PC 전용) + 수정/삭제
- "📥 엑셀로 전체 다운로드" 버튼 (SheetJS, 요약 시트 + 탭별 시트, 피드백 누적 포함)
- 리스크 탭 "플로우 스텝별 × 국가 리스크 카드" 자동 생성 (data.js의 RISKS 활용)

**디자인**
- 화이트 클린 + 단일 indigo 액센트 (다크+네온 도배 → 화이트로 전면 재작업)
- 게임 카드: 제목별 이모지 씬 (🏍️💨, ⚔️🛡️✨, 🐼🎋🍃 등) + 장르별 그라디언트
- 히어로: 컨테이너 안 다크 패널, 주변은 흰색 (EURATCHA 톤)
- MOLEP 로고에 두더지 마스코트 추가

**배포**
- 루트 `vercel.json`: `/` → `/auth/` 리다이렉트
- 회사 PC에 git 신원(`yourjini`) 로컬 한정 설정, GCM(Git Credential Manager)로 push 가능

**시연 시나리오**
- 차주 → 내일(2026-04-20) 회의로 변경. 시연자 PC localStorage에 피드백 누적, 회의 후 엑셀 다운로드로 회수.

### 2026-04-19 (세션 4 후반/야간) — 고려사항 페이지 본문 채움 (7개 탭, 113개 항목)
- 두더지 마스코트: 로고 🐭 제거 → 히어로 하단 SVG mole + CSS keyframe 땅굴 애니메이션 (3홀 루프)
- 상세 포맷 통일: <strong>핵심 이슈 / 배경·문제점 / 선택지 / 타사 대응 / 추천·액션</strong>
- 탭별 항목 detail 작성 완료 (총 113개):
  · 기획 15 / 정책 22 / 설계 15 / 개발 18 / 리스크 10 / 문의사항 16 / DB 17
  · QA 탭은 제목만 유지 (사용자 요청, 시연 후 보강 예정)
- 타사 대응 사례 인용: 넥슨, 스마일게이트, 엔씨, 카카오게임즈, 라이엇, 에픽,
  스팀, PUBG(크래프톤), 호요버스, Tencent, NetEase, 디스코드, 토스, 블리자드 등
  ※ 공개 자료 일반화 — 시연 전 정확성 재확인 필요

### 커밋 로그 (2026-04-19 세션)
```
ef2045e  feat: DB 탭 17개 항목 상세 (스키마/DDL/레지던시)
ef24abb  feat: 문의사항 탭 16개 항목 상세 (회의 논의 가이드)
5f96030  feat: 리스크 탭 10개 항목 상세 (법적 근거/리스크 크기/완화책)
d980925  feat: 개발 탭 18개 항목 상세 (아키/인증제공자/성능보안/스택)
ef164f3  feat: 설계/UX 탭 15개 항목 상세
97714c8  feat: 정책/약관 탭 22개 항목 상세
025daf1  feat: 두더지 땅굴 애니메이션 + 기획 탭 상세 15개
4bf8ab6  feat: 약관 패널/메모 스택/엑셀 다운로드/두더지 로고 + UX 폴리싱
1d6edae  refactor: 디자인 전면 화이트 클린 재작업
ee10f32  feat: 시연 프로토타입(auth/) v1 + 구 프로토타입 _legacy/ 이관
ca760f3  docs: 회사 PC 이어받기용 핸드오버 문서 추가
705ba11  feat: 한국어 고정 + 국가별 콘텐츠 필터링 + Vercel 배포 설정
fc91617  docs: 내부회의 확정사항 반영 + 초기 요건 정의서 v1.0
324bdd9  feat: MOLEP 글로벌 게임 포털 가입/로그인 플로우 프로토타입
```

### 시연 직전 체크리스트 (2026-04-20 회의 직전)
- [ ] https://molep.vercel.app/ 접속 → `/auth/`로 자동 리다이렉트 확인
- [ ] 상단 데모 바에서 5개국 스위칭 → 랜딩 콘텐츠·게임 카드 변경 확인
- [ ] 두더지 땅굴 애니메이션 정상 재생 (히어로 하단 3개 홀)
- [ ] 회원가입 9스텝 완주 (KR PASS/토스 → 7스텝, 다른 국가 → 9스텝)
- [ ] 약관 화면에서 각 약관 "보기" → 우측 패널 열림
- [ ] 고려사항 페이지 진입 → 8개 탭 전환 → "자세히 보기" 클릭 → 우측 패널
- [ ] 항목에 피드백 메모 입력 → 저장/수정/삭제 작동
- [ ] 📥 엑셀 다운로드 → 요약 시트 + 7개 탭 시트 (QA 포함) 확인
- [ ] 브라우저 전체화면(F11) + 줌 110~125%로 시연 가독성 점검
```

---

## 7. 유저 프로필 (이어받는 Claude가 반드시 알아야 할 것)

| 항목 | 내용 |
|---|---|
| 이름 | **최진희** |
| 이메일 | jinhee.choi@bpmg.biz |
| GitHub | yourjini |
| 직군 | **QA 담당자 (비개발자)** |
| 회사 | **BPMG (블로믹스)** — 게임 퍼블리셔 (※ 스마일게이트 아님) |
| 기술 수준 | git, 배포, 라이브러리 등 **기본 개념 낯섦** — 용어 설명 필수 |
| 작업 환경 | 집 PC + 회사 PC 2곳에서 작업 |
| 목표 | 이사님 보고/시연용 대시보드 제작 |

---

## 8. 회사/프로덕트 컨텍스트

- **블로믹스**: 게임 퍼블리셔 (스마일게이트 아님)
- **테일즈런너**: 라온엔터테인먼트 개발 / 블로믹스 퍼블리싱
- **으랏차**: 블로믹스 퍼블리싱 여러 게임의 통합 포털 (구축 중)
- **Watchtower**: 웹/어드민 QA 자동화 모니터링 대시보드 (별도 프로젝트)
- **MOLEP**: 본 프로젝트 — 글로벌 게임 포털 가입/로그인 플로우

> 프로덕트 언급 시 "블로믹스 퍼블리싱"으로 표현. 개발사/퍼블리셔 구분 유의.

---

## 9. 소통 스타일 & 피드백 룰 (필독)

### 9-1. 용어 설명
- 비개발자이므로 **git, 배포, 라이브러리, gitignore, pull, playwright 등 기본 용어**도 모르는 경우가 많음
- 새 개념이 나올 때마다 **한 줄 설명 + 비유/예시** 추가

### 9-2. 방향 논의 먼저
- **바로 코드부터 짜지 말 것**
- 작업 전 **선택지 제시 → 확인받기 → 진행**
- 유저는 고민/질문을 많이 하며 방향을 함께 결정하는 스타일

### 9-3. "여기까지 한 작업 저장" = 메모리 저장 요청
- 이 말이 나오면 memory 파일들 업데이트
- 단, 이어받는 Claude 계정은 별도 메모리 저장소이므로 **HANDOVER.md 또는 별도 문서에 반영 권장**

### 9-4. 매일 대화 시작 시 "어제 대화내용 참고해줘"
- 매일 대화 시작 시 이 요청이 자주 옴
- 이전 작업 맥락을 파악한 뒤 대화 이어가기
- 이어받는 Claude 계정에서는 본 문서(HANDOVER.md)와 `docs/requirements-v1.md`가 그 역할

### 9-5. 매일 README 수정이력 정리
- 대화 종료 시 또는 요청 시, README.md의 "수정 이력" 섹션에 당일 작업 날짜별 정리
- 시연 준비 및 이력 관리 목적
- dev + main 모두 반영 (해당되는 레포에서)
- ※ molep 레포에는 아직 README.md가 없으니, 필요 시 새로 만들거나 HANDOVER.md 하단에 추가

---

## 10. 기술 스택 & 운영 방식

- **프론트엔드**: Vanilla JS + HTML + CSS (프레임워크 없음)
- **배포**: Vercel (빌드 없이 정적 호스팅, SPA rewrite)
- **버전관리**: GitHub (main 브랜치 자동 배포)
- **빌드 단계 없음**: 수정 후 git push → Vercel 자동 반영

---

## 11. 다음 작업 후보 (미결정 — 유저와 확인 필요)

- [ ] 차주 회의 시연용 시나리오 정리 (스크립트/데모 순서)
- [ ] 플로우 A/B의 엣지 케이스 추가 (쿠키 탈취, VPN 접속 등)
- [ ] 어드민 화면 목업 (콘텐츠 국가별 노출 설정)
- [ ] 약관 템플릿 (KR/US/TW/JP/CN 5종)
- [ ] 실제 MaxMind API 연동 모킹
- [ ] QA 체크리스트 문서화
- [ ] README.md 작성 (수정 이력 섹션 포함)

---

## 12. 회사 PC 이어받기 시 주의사항

### 12-1. 쓰기 권한 없음 (읽기 전용)
- 현재 회사 PC에는 GitHub 인증을 설정하지 않는 방향
- 회사 PC에서 `git push`는 **인증 오류**로 실패할 것
- **해결**: 회사에서 수정한 내용은 ZIP 또는 diff로 저장 → 집 PC에서 반영 후 push
- 또는 회사에서 수정한 파일 내용을 대화로 공유 → 집에서 수동 반영

### 12-2. Claude 메모리는 계정/PC마다 별개
- `C:\Users\*\.claude\projects\...`는 로컬 파일
- 회사 PC의 다른 Claude 계정은 집 PC 메모리를 볼 수 없음
- **대체**: 본 HANDOVER.md가 그 역할을 대신함
- 이어받는 쪽에서 새로 얻은 중요한 결정/피드백은 **HANDOVER.md에 추가 기록** 권장 → 집에서 머지

### 12-3. 수정은 로컬에서, 배포는 집에서
- 회사 PC에서 `git clone` → 수정 → 로컬 확인
- 변경 파일을 집 PC로 전달 (이메일, 본인 Drive, USB 등)
- 집 PC에서 적용 → commit → push → Vercel 자동 배포

---

## 13. 핸드오버 체크리스트 (이어받는 쪽 첫 액션)

1. [ ] `git clone https://github.com/yourjini/molep.git`
2. [ ] 본 문서(`docs/HANDOVER.md`) 정독 — 섹션 7~9는 필독
3. [ ] `docs/requirements-v1.md` 정독
4. [ ] https://molep.vercel.app 접속하여 현재 프로토타입 동작 확인
5. [ ] 유저에게 "어떤 작업부터 진행할지" 확인 후 시작

---

## 14. 참조 리소스

- **요건 정의서**: `docs/requirements-v1.md`
- **관련 외부 문서 (집 PC only)**: `D:/MyC/old_backup/Gemini.txt` (초기 리서치 자료)
- **관련 게임 샘플 (집 PC only)**: `D:/MyC/old_backup/game/bubblebubble/`

---

*본 문서는 세션 간 연속성을 위한 스냅샷입니다. 최신 상태는 항상 `git log`와 실제 코드를 확인하세요.*
*업데이트 시 맨 위 작성일과 섹션 6(작업 이력)을 함께 갱신해주세요.*
